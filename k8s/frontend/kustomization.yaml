apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: product-platform

resources:
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: frontend
  app.kubernetes.io/part-of: cognitiva-solutions
  app.kubernetes.io/managed-by: kustomize