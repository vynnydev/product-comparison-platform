# REMOVA O EXAMPLE DO FINAL DO NOME DO ARQUIVO
apiVersion: v1
kind: Secret
metadata:
  name: slack-webhook
  namespace: security
  labels:
    app: alerting
type: Opaque
stringData:
  # Substitua pela URL real do Slack Webhook
  SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/TXXXXX/BXXXXX/XXXXXXXXXX"
  SLACK_CHANNEL: "#alerts-infrastructure"