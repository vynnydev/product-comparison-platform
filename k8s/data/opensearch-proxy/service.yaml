apiVersion: v1
kind: Service
metadata:
  name: opensearch-proxy
  namespace: data
  labels:
    app: opensearch-proxy
spec:
  type: ClusterIP
  selector:
    app: opensearch-proxy
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP